<!DOCTYPE html>
<html>
  <head>
    <title>Claire VOijant</title>

    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>

  </head>

  <body>
  	<div class="container">
  	    <nav class="navbar navbar-fixed-top" id="top-nav">
        <div class="navbar-header">
          <a class="navbar-brand" href="#" id="title">Claire VOijant</a>
        </div>
        <ul class="nav navbar-right" id="top-menu">
          <% if user_signed_in? %>
        <button class="logout">
          <li><span class="glyphicon glyphicon-log-out" aria-hidden="true"></span> <a href="#" style="color:#ffffff!important;font-weight:none!important"><%= link_to "Log Out", destroy_user_session_path, :method => :delete %></a></li></button>
        </ul>
        <% else %>
        <button class="logout">
          <li><span class="glyphicon glyphicon-log-in" aria-hidden="true"></span> <a href="/users/sign_in" style="color:#ffffff!important;font-weight:none!important">Log In</a></li></button>
        </ul>
        <% end %>
      </div>
    </nav>
    <br/><br/>
    <%= yield %>
  </body>
</html>
